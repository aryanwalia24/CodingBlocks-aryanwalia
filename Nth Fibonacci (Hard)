#include<stdio.h>

int fibbo(int n){
    if (n<2)
    {
        return n;
    }
    return fibbo(n-1)+fibbo(n-2);
    
}
int main(){ 
    int a=0,b=1,n;
    scanf("%d",&n);
    for (int i = n; i <= n; i++)
    {
        printf("%d ",fibbo(i));

    }
    
    return 0; 
}
