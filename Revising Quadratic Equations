/*made by Aryan Walia*/
#include<stdio.h>
#include<math.h>
int main(void) {
	int a,b,c;
	scanf("%d %d %d",&a,&b,&c);
	int d=(b*b)-4*a*c;
	int x,y;
	x=((-b)+sqrt(d))/(2*a);
	y=((-b)-sqrt(d))/(2*a);


	if(d<0){
		printf("Imaginary");
	}
	if(d==0){
		printf("Real and Equal\n");
		printf("%d %d",y,x);
	}
	
	if(d>0){
		printf("Real and Distinct\n");
		printf("%d %d",y,x);
	}
	return 0;
}
